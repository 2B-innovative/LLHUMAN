# Optional token for the browser-based human operator console
OPERATOR_TOKEN=change-me

# Optional bearer token for /v1/* clients
LLHUMAN_API_KEY=change-me-too

# How long clients may wait for biological inference
HUMAN_TIMEOUT_SECONDS=900
